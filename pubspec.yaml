name: bill_buddy
description: Lightweight expense splitting app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase (supports Android, iOS, and Web)
  firebase_core: ^4.1.1        # Updated for web compatibility
  cloud_firestore: ^6.0.2      # Updated for web compatibility
  
  # UI
  google_fonts: ^6.1.0
  
  # Utilities
  uuid: ^4.2.2
  intl: ^0.20.2               # Updated version
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
